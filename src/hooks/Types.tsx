/**
 * Datos del deseo.
 * @category Types
 */
export interface WishType {
  /** Identificador del deseo. */
  id: string;
  /** Estado de realización del deseo. */
  done: boolean;
  /** Texto del deseo. */
  text: string;
}
